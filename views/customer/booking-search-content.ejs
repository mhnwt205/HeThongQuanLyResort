<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0"><i class="fas fa-search me-2"></i>Tra cứu đặt phòng</h4>
                </div>
                <div class="card-body">
                    <p class="text-muted mb-4">Nhập mã đặt phòng để xem thông tin chi tiết</p>
                    
                    <form id="searchForm">
                        <div class="mb-3">
                            <label for="bookingCode" class="form-label">Mã đặt phòng</label>
                            <input type="text" class="form-control" id="bookingCode" name="bookingCode" 
                                   placeholder="VD: BK2412010001" required>
                            <div class="form-text">Mã đặt phòng được gửi qua email sau khi đặt phòng thành công</div>
                        </div>
                        
                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary btn-lg">
                                <i class="fas fa-search me-2"></i>Tra cứu đặt phòng
                            </button>
                        </div>
                    </form>
                </div>
            </div>

            <!-- Help Section -->
            <div class="card mt-4">
                <div class="card-body">
                    <h5><i class="fas fa-question-circle me-2"></i>Bạn cần hỗ trợ?</h5>
                    <p class="mb-3">Nếu bạn không tìm thấy mã đặt phòng hoặc gặp khó khăn, hãy liên hệ với chúng tôi:</p>
                    
                    <div class="row">
                        <div class="col-md-6">
                            <h6><i class="fas fa-phone me-2"></i>Hotline</h6>
                            <p class="mb-1"><strong>1900 1234</strong></p>
                            <small class="text-muted">24/7</small>
                        </div>
                        <div class="col-md-6">
                            <h6><i class="fas fa-envelope me-2"></i>Email</h6>
                            <p class="mb-1"><strong>booking@paradise-resort.com</strong></p>
                            <small class="text-muted">Phản hồi trong 30 phút</small>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
    document.getElementById('searchForm').addEventListener('submit', function(e) {
        e.preventDefault();
        
        const bookingCode = document.getElementById('bookingCode').value.trim();
        
        if (!bookingCode) {
            alert('Vui lòng nhập mã đặt phòng');
            return;
        }

        // Redirect to booking detail page
        window.location.href = `/booking/detail/${bookingCode}`;
    });
});
</script>

